import front.Fenetre;

public class App {
    public static void main(String[] args) throws Exception {
        Fenetre f = new Fenetre();
        f.setVisible(true);
        // double angle = Math.toRadians(20);
        // System.out.println(Math.tan(angle));


        // double w = 100;
        // double hypo =  w / Math.cos(angle);
        // double coefDirecteur =  Math.tan(angle);
        // double yfin = Math.sqrt( Math.pow(hypo, 2) - Math.pow(w, 2) );
        // double b = yfin - coefDirecteur * w;

        // System.out.println("y = " + coefDirecteur + " x + " + b);
    }
}
